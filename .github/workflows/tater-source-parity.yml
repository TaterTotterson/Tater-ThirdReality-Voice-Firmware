name: Tater Linux Update Check

on:
  schedule:
    - cron: "17 13 * * 1"
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Report available Tater Linux updates
        run: ./script/update_tater_linux_source.sh --report
